import styled from "styled-components";

export const Container = styled.div`
  display: flex;
`;

export const ContainerMain = styled.main`
  display: flex;
  padding: 16px;
  margin-top: 60px;
  flex: 1;
  background-color: var(--bs-cyan-dark);
  min-height: calc(100vh - 60px);
`;
